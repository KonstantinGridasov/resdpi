plugins {
    id 'com.android.library'
    id 'maven-publish'
}

android {
    namespace 'com.gkreduction.resdpi'
    compileSdk = 35
    resourcePrefix = "reslib_"
    defaultConfig {
        minSdk = 24
    }

    buildTypes {
        release {
        }
    }
}

dependencies {
}
afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
//                from components.release
                groupId = 'com.github.KonstantinGridasov'
                artifactId = 'resdpi'
                version = '1.0.0'
            }
        }
    }
}