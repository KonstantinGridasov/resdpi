plugins {
    id 'com.android.library'
}

android {
    namespace 'com.gkreduction.resdpi'
    compileSdk = 35 // Укажите вашу версию SDK

    defaultConfig {
        minSdk = 24
    }

    buildTypes {
        release {
        }
    }
}

dependencies {
}